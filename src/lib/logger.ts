export function debug(message: any, ...optional) {
    console.debug(message, ...optional);
}

export function info(message: any, ...optional) {
    console.info(message, ...optional);
}

export function warn(message: any, ...optional) {
    console.warn(message, ...optional);
}

export function error(message: any, ...optional) {
    console.error(message, ...optional);
}
